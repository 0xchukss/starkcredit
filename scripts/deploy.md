# StarkCredit Sepolia deployment quickstart

```bash
cd contracts
scarb build
# deploy with your preferred starknet CLI flow
```

Then configure `frontend/.env.local` and run:

```bash
cd frontend
npm install
npm run dev
```
